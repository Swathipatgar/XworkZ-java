class Bill {
 //states /var/ref/properties of bill 16 classes, 25 variables 20 copies , intat  init then print 
 //non static 
    int billId;
    double billFees;
    boolean isFinalBill;
    boolean isLatestBill;
    String billIssueDate;
    String billDueDate;
    boolean billOverDue;
    String billUsnNo;

    // Default Constructor
    Bill() {
        System.out.println("Bill object is created");
		
		sop (is bill)
		bill.billID =1;
		bill.siLatestBill =true;
		2nd before nd after
		sop (before execution);
		sop(bill)
		Bill bill =new Bill();
		sop(bill)
		sop (after execution)
		
    }
}